# FileReleaser
A tool  to fix file in use deletion failure and occupied USB drive  ejection issues.

## Libs
`pip install psutil pyinstaller`

## packing command
`pyinstaller --uac-admin -F -w -i avatar.ico --add-data "avatar.ico;. main.py"`
